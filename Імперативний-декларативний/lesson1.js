// const arr = [1,2,3,4,5,6,7,8,9]
// const test = arr.forEach((item,index,arrr)=>{console.log(arrr);})

// масив.метод(()=>{})
//     масив.метод((item, idx, arr)=> item)
//     const dataArray = [      
//   {
//     id: 101,
//     name: "Олег",
//     isActive: true,
//   },
//   {
//     id: 102,
//     name: "Марія",
//     isActive: false,
//   },
//   {
//     id: 103,
//     name: "Віктор",
//     isActive: true,
//   },
//   {
//     id: 104,
//     name: "Тетяна",
//     isActive: true,
//   },
//   {
//     id: 105,
//     name: "Андрій",
//     isActive: false,
//   },
// ];
// for(let i = 0;i < dataArray.length;i++){
//     console.log(dataArray[i]);
// }
// dataArray.forEach((item)=>console.log(item))
// нічого не повертає і може дефікувати



// метод Map: переберає,повертає новий масив,де до кожного елемента щось застосує
// const arr = [1,2,3,4,5,6,7,8,9]
// const arrMap = arr.map((item)=>item * 10)
// // console.log(arrMap);


// // //
map(
// Створює новий масив.
// Перетворює кожен елемент.
// Довжина така сама.
// “Пройтись по всіх і щось змінити.”
)

forEach(
// Просто перебирає масив.
// Нічого не повертає (undefined).
// “Зробити дію для кожного, але масив не змінює.”
)

filter(
// Повертає масив, де елементи пройшли умову.
// “Відфільтруй тих, хто підходить.”
)

find(
// Повертає перший елемент, який підходить.
// Або undefined.
// “Знайди першого.”
)

every(
// Перевіряє, чи ВСІ елементи виконують умову.
// Повертає true/false.
// “Чи всі такі?”
)

some(
// Перевіряє, чи ХОЧА Б ОДИН елемент виконує умову.
// Повертає true/false.
// “Чи є хоч один такий?”
)



// ⚡ Короткі приклади (для пам’яті)
arr.map(x => x * 2)         // [перероблений масив]
arr.forEach(x => console.log(x))  // просто перебір
arr.filter(x => x > 10)     // масив елементів > 10
arr.find(x => x === 5)      // перший елемент === 5
arr.every(x => x > 0)       // перевіряє ВСІ > 0
arr.some(x => x < 0)        // чи Є хоча б один < 0

// 🧠 СУПЕРКРАТКА СХЕМА

// map → переробити всіх

// forEach → просто пройтись

// filter → вибрати багатьох

// find → знайти одного

// every → всі підходять?

// some → хоч один підходить?