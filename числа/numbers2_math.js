const max = Math.max(22,55,12,54,13,75,23,6);
console.log(max);

const min = Math.min(2,45,5,2,5,6,4,3,-5,33);
console.log(min);

const step = Math.pow(5 , 2)
console.log(step);

const bow = Math.floor(12.34)
console.log(bow);

const cale = Math.ceil(12.34)
console.log(cale);

const round = Math.round(12.34)
console.log(round);

const skrt = Math.sqrt(25)
console.log(skrt);

const random = Math.random() * (100 - 0) 
console.log(Math.floor(random));

// Напишіть скрипт, який знаходить площу прямокутника. Висота 23см, шириною 10см - 
// всі данні повинні зберігатися в змінних.  
// Значення площі повинно зберігається в числовій змінній.
const heit = '23sm';
const width = '10sm';
const heitInt = parseInt(heit)
const idthInt = parseInt(width)
const result = heitInt * idthInt
console.log(result);
